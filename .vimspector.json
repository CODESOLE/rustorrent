{
  "configurations": {
    "Launch with args: sample.torrent": {
      "adapter": "CodeLLDB",
      "configuration": {
        "type": "rust",
        "request": "launch",
        "program": "${workspaceRoot}/target/debug/rustorrent",
        "cwd": "${workspaceRoot}",
        "env": {
          "BASEPATH": "${workspaceRoot}"
        },
        "args": [
          "torrents/sample.torrent"
        ]
      }
    }
  }
}
